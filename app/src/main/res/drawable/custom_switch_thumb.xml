<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the switch is checked -->
    <item android:state_checked="true">
        <shape android:shape="oval">
            <size android:width="24dp" android:height="24dp" />
            <solid android:color="#FFFFFF" /> <!-- White color when ON -->
            <stroke android:width="1dp" android:color="#B0B0B0" /> <!-- Light border -->
        </shape>
    </item>
    <!-- State when the switch is not checked -->
    <item>
        <shape android:shape="oval">
            <size android:width="24dp" android:height="24dp" />
            <solid android:color="#FFFFFF" /> <!-- White color when OFF -->
            <stroke android:width="1dp" android:color="#B0B0B0" /> <!-- Light border -->
        </shape>
    </item>
</selector>
